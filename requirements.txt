# MISP Installation Tool - Optional Python Dependencies
# Install with: pip3 install -r requirements.txt

# YAML configuration file support (optional)
pyyaml>=6.0

# Note: The script will work without these packages,
# but YAML config file support will be disabled.
# You can still use JSON config files without any dependencies.